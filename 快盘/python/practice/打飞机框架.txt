加载背景音乐
播放背景音乐（设置单曲循环）
我方飞机诞生
interval=0

while=true:
	if press the close button:
	exit
	
	interval+=1
	if interval==50
		interval=0
		小飞机诞生

		little planes move a step
		refresh display
		
		if  user moves mouse
		my plane's position= mouse position
		
	if  my plane crashes with little plane:
	    i died, playing the background music
		edit the pattern of my plane
		print 'game over'
		stop the music (fade away)
